# Python dependencies for the Advanced Reverse Engineering Toolkit
# Install:  pip install -r requirements.txt

# HTTP for sourcemap fetch and remote pentest probes
requests>=2.31.0
urllib3>=2.0.0

# YAML parsing for agent manifest validation (CI/install scripts)
pyyaml>=6.0

# Optional: progress bars for long-running unpack operations
tqdm>=4.65.0

# PyInstaller unpacking
# pyinstxtractor-ng  # pip install pyinstxtractor-ng

# .NET decompilation (requires .NET SDK for ilspycmd)
# ilspycmd: dotnet tool install -g ilspycmd

# Network traffic interception
# mitmproxy>=10.0.0  # pip install mitmproxy (large dependency)

# Dynamic instrumentation
# frida-tools>=12.0.0  # pip install frida-tools

# Memory dump scanning (no extra deps — uses ctypes/subprocess)
